# 博实智能巡检平台 - Cursor AI 规则

## 项目概述
这是一个基于 FastAPI 和 ROS2 的智能巡检系统数据中台，采用现代化的分层架构设计，支持用户认证、权限管理、ROS2 通信和 WebSocket 实时通信。

## 技术栈
- **后端框架**: FastAPI 0.104.1
- **数据库**: MySQL + SQLAlchemy (异步)
- **认证**: JWT + Session 管理
- **缓存**: Redis
- **机器人通信**: ROS2 (humble)
- **实时通信**: WebSocket
- **开发环境**: WSL2 + Python 3.10

## 项目架构

### 分层架构
```
API Layer (app/api/) → Service Layer (app/services/) → Repository Layer (app/repositories/) → Model Layer (app/models/)
```

### 目录结构说明
- `app/api/v1/`: API 路由层，处理 HTTP 请求
- `app/services/`: 业务逻辑层，处理复杂业务规则
- `app/repositories/`: 数据访问层，封装数据库操作
- `app/models/`: 数据模型层，SQLAlchemy ORM 模型
- `app/schemas/`: Pydantic 数据验证模型
- `app/core/`: 核心功能（认证、权限、异常等）
- `app/config/`: 配置管理
- `app/ros/`: ROS2 集成模块
- `app/websocket/`: WebSocket 通信模块
- `app/utils/`: 工具函数

## 编码规范

### Python 代码风格
- 遵循 PEP 8 标准
- 使用 type hints
- 函数和类必须有文档字符串
- 变量命名使用 snake_case
- 类名使用 PascalCase
- 常量使用 UPPER_CASE

### FastAPI 特定规范
- API 路由函数必须有详细的文档字符串和参数说明
- 使用 Pydantic 模型进行请求/响应验证
- 异步函数使用 `async/await`
- 依赖注入使用 `Depends()`
- 错误处理使用自定义异常类

### 数据库相关
- 使用 SQLAlchemy 2.0 异步语法
- 模型继承 `BaseModel`
- 使用 Repository 模式封装数据访问
- 所有数据库操作必须在事务中进行

### 认证和权限
- 使用 JWT 令牌进行认证
- 基于角色的权限控制 (RBAC)
- 角色层级: super_admin > admin > operator > viewer > user
- 使用装饰器进行权限检查

## 代码生成指导

### API 路由生成
当创建新的 API 路由时：
```python
from fastapi import APIRouter, Depends, HTTPException
from sqlalchemy.ext.asyncio import AsyncSession
from ...core.deps import get_db
from ...core.permissions import require_admin
from ...services.example_service import ExampleService
from ...schemas.example import ExampleCreate, ExampleResponse

router = APIRouter()

@router.post("/", response_model=ExampleResponse)
async def create_example(
    data: ExampleCreate,
    db: AsyncSession = Depends(get_db),
    current_user: dict = Depends(require_admin)
):
    """创建示例
    
    Args:
        data: 创建数据
        db: 数据库会话
        current_user: 当前用户
    
    Returns:
        创建的示例对象
    """
    service = ExampleService(db)
    return await service.create(data, current_user)
```

### Service 层生成
```python
from typing import Dict, Any
from sqlalchemy.ext.asyncio import AsyncSession
from fastapi import HTTPException
from ..repositories.example_repository import ExampleRepository
from ..schemas.example import ExampleCreate

class ExampleService:
    """示例业务逻辑服务"""
    
    def __init__(self, db: AsyncSession):
        self.db = db
        self.repo = ExampleRepository(db)
    
    async def create(self, data: ExampleCreate, user: dict) -> Dict[str, Any]:
        """创建示例"""
        # 业务逻辑验证
        if await self.repo.exists_by_name(data.name):
            raise HTTPException(status_code=400, detail="名称已存在")
        
        # 创建数据
        create_data = {
            **data.dict(),
            "created_by": user["username"]
        }
        
        example = await self.repo.create(create_data)
        return {"message": "创建成功", "data": example}
```

### Repository 层生成
```python
from typing import List, Optional, Dict, Any
from sqlalchemy.ext.asyncio import AsyncSession
from sqlalchemy import select
from ..models.example import Example

class ExampleRepository:
    """示例数据仓库"""
    
    def __init__(self, db: AsyncSession):
        self.db = db
    
    async def create(self, data: Dict[str, Any]) -> Example:
        """创建示例"""
        example = Example(**data)
        self.db.add(example)
        await self.db.commit()
        await self.db.refresh(example)
        return example
    
    async def get_by_id(self, id: str) -> Optional[Example]:
        """根据ID获取示例"""
        stmt = select(Example).where(Example.id == id, Example.is_deleted == False)
        result = await self.db.execute(stmt)
        return result.scalar_one_or_none()
```

## 统一错误码规范

### 错误码定义
- **1XX**: 表示请求失败，请求数据有误
- **200**: 表示请求成功
- **210**: 登录成功，但是需要重置密码
- **301**: 请求参数错误
- **4XX**: 表示请求失败，用户无权限或其他
- **410**: token验证失败（过期、无效、不存在等）
- **414**: 登录失败
- **999**: 表示系统异常

### 错误处理模式
```python
from ...core.exceptions import (
    ValidationError, BusinessError, TokenError, LoginFailedError,
    PermissionDeniedError, ResourceNotFoundError, SystemError
)
from ...utils.response import ApiResponse

# 数据验证错误 - 具体信息在message中
raise ValidationError("用户名格式不正确，只能包含字母和数字")
raise ValidationError("密码修改失败，新密码不能与旧密码相同")

# 业务逻辑错误 - 具体信息在message中
raise BusinessError(f"用户名 '{username}' 已存在")

# Token相关错误 - 具体信息在message中
raise TokenError("用户登录信息过期，需要重新登录")
raise TokenError("不合法的token")

# 登录失败 - 具体信息在message中
raise LoginFailedError("用户名或密码错误")

# 权限不足 - 具体信息在message中
raise PermissionDeniedError("当前用户无权限删除其他管理员")

# 资源不存在 - 具体信息在message中
raise ResourceNotFoundError(f"用户 '{user_id}' 不存在")

# 系统异常 - 具体信息在message中
raise SystemError("数据库连接超时")

# 成功响应
return ApiResponse.success(data=result, message="操作成功")

# 分页响应
return ApiResponse.paginated(items=users, total=total, page=page, size=size)
```

### 统一异常处理
```python
from ...core.exceptions import (
    ValidationError, BusinessError, TokenError, LoginFailedError,
    PermissionDeniedError, ResourceNotFoundError, SystemError
)
from ...utils.response import ApiResponse

# 数据验证错误 - 具体信息在message中
raise ValidationError("用户名格式不正确，只能包含字母和数字")
raise ValidationError("密码修改失败，新密码不能与旧密码相同")

# 业务逻辑错误 - 具体信息在message中
raise BusinessError(f"用户名 '{username}' 已存在")

# Token相关错误 - 具体信息在message中
raise TokenError("用户登录信息过期，需要重新登录")
raise TokenError("不合法的token")

# 登录失败 - 具体信息在message中
raise LoginFailedError("用户名或密码错误")

# 权限不足 - 具体信息在message中
raise PermissionDeniedError("当前用户无权限删除其他管理员")

# 资源不存在 - 具体信息在message中
raise ResourceNotFoundError(f"用户 '{user_id}' 不存在")

# 系统异常 - 具体信息在message中
raise SystemError("数据库连接超时")

# 成功响应
return ApiResponse.success(data=result, message="操作成功")

# 分页响应
return ApiResponse.paginated(items=users, total=total, page=page, size=size)
```

### 权限检查
```python
from ...core.permissions import require_admin, can_manage_role
from ...core.exceptions import PermissionDeniedError

# 需要管理员权限
@router.post("/admin-only")
async def admin_function(current_user: dict = Depends(require_admin)):
    pass

# 检查角色管理权限
if not can_manage_role(current_user["role"], target_role):
    raise PermissionDeniedError("权限不足，无法管理该角色用户")
```

### ROS2 集成
```python
# 获取 ROS2 Bridge
def get_ros2_bridge() -> ROS2Bridge:
    from ...main import app
    ros2_bridge = app.state.ros2_bridge
    if not ros2_bridge:
        raise HTTPException(status_code=500, detail="ROS2 Bridge未初始化")
    return ros2_bridge

# 发布消息
ros2_bridge = get_ros2_bridge()
ros2_bridge.publish_string("hello", "/test_topic")
```

### WebSocket 处理
```python
# WebSocket 消息处理
async def handle_websocket_message(websocket: WebSocket, message: dict):
    message_type = message.get("type")
    if message_type == "ping":
        await websocket.send_json({"type": "pong"})
```

## 文件命名约定
- API 文件: `{resource}.py` (如 `users.py`, `auth.py`)
- Service 文件: `{resource}_service.py`
- Repository 文件: `{resource}_repository.py`
- Model 文件: `{resource}.py` (单数形式)
- Schema 文件: `{resource}.py` (单数形式)

## 导入顺序
1. 标准库导入
2. 第三方库导入
3. FastAPI 相关导入
4. SQLAlchemy 相关导入
5. 项目内部导入 (使用相对导入)

## 测试指导
- 单元测试文件: `test_{module}.py`
- 集成测试文件: `test_integration_{feature}.py`
- 使用 pytest 和 pytest-asyncio
- 测试数据使用工厂模式

## 安全考虑
- 所有 API 都需要认证 (除了登录接口)
- 敏感操作需要权限验证
- 输入数据必须验证
- 数据库查询防止 SQL 注入
- 密码必须哈希存储

## 性能优化
- 数据库查询使用索引
- 大量数据使用分页
- 缓存频繁查询的数据
- 异步处理 I/O 密集操作

## 统一日志系统

### 日志获取方式
```python
from app.config.logging import get_logger
logger = get_logger(__name__)
```

### 日志记录规范
- **使用统一的logger获取方式**: `get_logger(__name__)`
- **结构化日志记录**: 使用 `extra` 参数添加上下文信息
- **不记录敏感信息**: 密码、token、个人隐私信息
- **使用适当的日志级别**: DEBUG < INFO < WARNING < ERROR < CRITICAL

### 日志使用示例
```python
# 基本日志记录
logger.info("用户登录成功")
logger.warning("配置文件缺少某个选项，使用默认值")
logger.error("数据库连接失败")

# 结构化日志记录
logger.info(
    "用户执行操作",
    extra={
        "user_id": user.id,
        "operation": "create_post",
        "ip_address": request.client.host,
        "request_id": getattr(request.state, 'request_id', None)
    }
)

# 异常日志记录
try:
    result = risky_operation()
except Exception as e:
    logger.error(
        "操作失败: %s",
        str(e),
        extra={
            "operation": "risky_operation",
            "input_data": input_data,
            "user_id": user.id
        },
        exc_info=True  # 自动包含异常堆栈
    )
    raise
```

### 日志文件管理
- **按日期分级**: 日志自动按日期存储在 `logs/YYYY-MM-DD/` 目录
- **分类存储**: 
  - `app.log`: 主日志文件
  - `app_error.log`: 错误日志文件
  - `app_debug.log`: 调试日志文件（仅开发环境）
- **自动轮转**: 文件大小限制10MB，保留3个备份
- **日志清理**: 使用 `python3 scripts/log_manager.py cleanup` 清理旧日志

## 部署相关
- 使用 Docker 容器化
- 环境变量管理配置
- 健康检查端点
- 优雅关闭处理

当你需要添加新功能时，请遵循以上架构和规范，确保代码的一致性和可维护性。
