# Docker镜像打包说明（自用）

## 📦 快速打包流程

### 1. 确认配置文件

#### `.env.backup` 文件配置
```bash
# 确保以下配置正确
DEBUG=true                    # 开启调试和文档
DATABASE_URL=mysql+aiomysql://boshi:Xx147258.@mysql:3306/boshirobot  # 使用Docker服务名
REDIS_URL=redis://redis:6379/0  # 使用Docker服务名
DB_PASSWORD=Xx147258.         # 数据库密码
```

#### `docker-compose.yml` 配置
```yaml
# 确保MySQL配置正确
MYSQL_PASSWORD: ${DB_PASSWORD:-Xx147258.}  # 默认密码
ports:
  - "3307:3306"  # 避免端口冲突

# 确保应用配置正确
DEBUG: "true"  # 启用文档（或使用 ${DEBUG:-true}）
```

#### `requirements.txt` 关键依赖
```txt
bcrypt==4.1.3      # 必须！5.0.0 与 passlib 不兼容
passlib[bcrypt]==1.7.4
numpy==1.24.3      # ROS2 需要
```

---

## 🚀 打包命令

### Windows PowerShell 中执行：

```powershell
# 1. 构建Docker镜像
docker build -f docker/Dockerfile -t boshi-inspection-platform:1.0 .

# 2. 保存为tar文件
docker save boshi-inspection-platform:1.0 -o packages/boshi-inspection-platform-1.0.tar

# 3. 创建部署目录
$deployPath = "$env:USERPROFILE\Desktop\boshi-inspection-deploy"
New-Item -Path $deployPath -ItemType Directory -Force | Out-Null

# 4. 复制文件到部署包
Copy-Item packages/boshi-inspection-platform-1.0.tar $deployPath -Force
Copy-Item docker/docker-compose.yml $deployPath -Force
Copy-Item .env.backup "$deployPath\.env" -Force
Copy-Item scripts/init_database.sql $deployPath -Force
Copy-Item packages/快速部署指南.md $deployPath -Force

# 5. 验证打包内容
Get-ChildItem $deployPath | Select-Object Name, @{Name="Size(MB)";Expression={if($_.PSIsContainer){"-"}else{[math]::Round($_.Length/1MB,2)}}}

# 6. 压缩为zip文件（方便传输）
cd "$env:USERPROFILE\Desktop"
Compress-Archive -Path "boshi-inspection-deploy\*" -DestinationPath "boshi-inspection-deploy.zip" -Force

# 7. 查看最终打包文件
Get-Item "boshi-inspection-deploy.zip" | Select-Object Name, @{Name="Size(MB)";Expression={[math]::Round($_.Length/1MB,2)}}
```

---

## 🔧 一键打包脚本

创建 `build-and-package.ps1`：

```powershell
# 博实智能巡检平台 - 一键打包脚本

Write-Host "开始打包..." -ForegroundColor Green

# 1. 构建镜像
Write-Host "`n[1/5] 构建Docker镜像..." -ForegroundColor Cyan
docker build -t boshi-inspection-platform:1.0 .
if ($LASTEXITCODE -ne 0) {
    Write-Host "镜像构建失败！" -ForegroundColor Red
    exit 1
}

# 2. 保存镜像
Write-Host "`n[2/5] 保存镜像为tar文件..." -ForegroundColor Cyan
docker save boshi-inspection-platform:1.0 -o packages/boshi-inspection-platform-1.0.tar

# 3. 创建部署目录
$deployPath = "$env:USERPROFILE\Desktop\boshi-inspection-deploy"
Write-Host "`n[3/5] 创建部署目录..." -ForegroundColor Cyan
New-Item -Path $deployPath -ItemType Directory -Force | Out-Null

# 4. 复制文件
Write-Host "`n[4/5] 复制文件到部署包..." -ForegroundColor Cyan
Copy-Item packages/boshi-inspection-platform-1.0.tar $deployPath -Force
Copy-Item docker-compose.yml $deployPath -Force
Copy-Item .env.backup "$deployPath\.env" -Force
Copy-Item scripts/init_database.sql $deployPath -Force
Copy-Item packages/Docker部署指南.md $deployPath -Force

# 5. 验证
Write-Host "`n[5/5] 验证部署包..." -ForegroundColor Cyan
Get-ChildItem $deployPath | Select-Object Name, @{Name="Size(MB)";Expression={if($_.PSIsContainer){"-"}else{[math]::Round($_.Length/1MB,2)}}} | Format-Table

Write-Host "`n✅ 打包完成！部署包位置：" -ForegroundColor Green
Write-Host $deployPath -ForegroundColor Yellow
```

**使用方法**：
```powershell
.\build-and-package.ps1
```

---

## ⚠️ 常见问题

### 1. 环境变量优先级

**Docker环境变量优先级（从高到低）**：
```
1. docker-compose.yml 中直接定义的 environment
2. docker-compose.yml 中引用的 .env 文件（与 docker-compose.yml 同目录）
3. 镜像内的 /app/.env 文件（最低）
```

**示例**：

```yaml
# docker-compose.yml

# 方式1：直接定义（最高优先级，会覆盖一切）
environment:
  DEBUG: "true"  # 固定值，无法被外部.env覆盖

# 方式2：引用外部.env（推荐，灵活）
environment:
  DEBUG: ${DEBUG:-true}  # 从外部.env读取，默认true
```

**当前配置问题**：
- `docker-compose.yml` 中是 `DEBUG: "true"` (直接定义)
- 这会覆盖镜像内的 `.env` 文件
- 如果要让外部 `.env` 生效，应改为 `DEBUG: ${DEBUG:-true}`

**建议修改**：
```yaml
environment:
  DEBUG: ${DEBUG:-true}
  DB_PASSWORD: ${DB_PASSWORD:-Xx147258.}
  SECRET_KEY: ${SECRET_KEY:-change-this-secret-key}
```

### 2. bcrypt 版本问题

**症状**：登录接口返回 500，错误信息 `password cannot be longer than 72 bytes`

**原因**：bcrypt 5.0.0 与 passlib 1.7.4 不兼容

**解决**：
```txt
# requirements.txt
bcrypt==4.1.3  # 必须指定此版本
```

### 3. 数据库连接失败

**症状**：应用容器不断重启，日志显示 `Can't connect to MySQL server on 'localhost'`

**原因**：容器内不能使用 `localhost`

**解决**：
```env
# .env.backup
DATABASE_URL=mysql+aiomysql://boshi:Xx147258.@mysql:3306/boshirobot  # 使用服务名 mysql
REDIS_URL=redis://redis:6379/0  # 使用服务名 redis
```

### 4. 端口冲突

**症状**：MySQL容器无法启动，提示端口占用

**解决**：
```yaml
# docker-compose.yml
mysql:
  ports:
    - "3307:3306"  # 宿主机用3307，容器内仍是3306
```

### 5. 文档无法访问

**症状**：访问 `/docs` 返回 404

**原因**：DEBUG=false

**解决**：
```env
# .env.backup 或 docker-compose.yml
DEBUG=true
```

---

## ✅ 打包前检查清单

- [ ] `.env.backup` 中数据库使用 `mysql` 服务名（非localhost）
- [ ] `.env.backup` 中Redis使用 `redis` 服务名（非localhost）
- [ ] `.env.backup` 中 `DEBUG=true`
- [ ] `.env.backup` 中密码为 `Xx147258.`
- [ ] `docker-compose.yml` 中MySQL端口为 `3307:3306`
- [ ] `docker-compose.yml` 中密码为 `Xx147258.`
- [ ] `requirements.txt` 中 `bcrypt==4.1.3`
- [ ] `requirements.txt` 中包含 `numpy==1.24.3`
- [ ] `app/repositories/robot_repository.py` 中有 `get_all` 方法

---

## 📝 打包后验证

### 验证镜像内容
```powershell
# 查看镜像内.env文件
docker run --rm boshi-inspection-platform:1.0 cat /app/.env | Select-String "DEBUG|DATABASE_URL"

# 查看依赖版本
docker run --rm --entrypoint pip boshi-inspection-platform:1.0 list | Select-String "bcrypt|passlib|numpy"
```

**期望输出**：
```
DEBUG=true
DATABASE_URL=mysql+aiomysql://boshi:Xx147258.@mysql:3306/boshirobot

bcrypt             4.1.3
passlib            1.7.4
numpy              1.24.3
```

---

## 🎯 版本历史

### v1.0 (2025-10-17)
- ✅ 修复 bcrypt 版本兼容性问题（降级到4.1.3）
- ✅ 启用 API 文档（DEBUG=true）
- ✅ 修复数据库配置（使用Docker服务名）
- ✅ 修改密码为安全密码（Xx147258.）
- ✅ 修复MySQL端口冲突（改为3307）
- ✅ 添加 numpy 依赖（1.24.3）
- ✅ 修复 RobotRepository.get_all 方法缺失

---

**最后更新**: 2025-10-17  
**镜像大小**: ~315 MB  
**部署环境**: 银河麒麟 V10 + Docker 28.2.2

