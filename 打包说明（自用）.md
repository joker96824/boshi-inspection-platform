cd D:\Project\boshi\boshi-inspection-platform; 
docker build -f docker/Dockerfile -t boshi-inspection-platform:1.0 .


cd D:\Project\boshi\boshi-inspection-platform; 
docker save boshi-inspection-platform:1.0 -o packages/boshi-inspection-platform-1.0.tar

cd D:\Project\boshi\boshi-inspection-platform; $deployPath = "$env:USERPROFILE\Desktop\boshi-inspection-deploy"; if (Test-Path $deployPath) { Remove-Item $deployPath -Recurse -Force }; New-Item -Path $deployPath -ItemType Directory -Force | Out-Null; Copy-Item packages/boshi-inspection-platform-1.0.tar $deployPath -Force; Copy-Item docker/docker-compose.yml $deployPath -Force; if (Test-Path .env.example) { Copy-Item .env.example "$deployPath\.env.example" -Force } else { Write-Host "⚠️  .env.example 文件不存在，请手动创建环境变量配置文件" -ForegroundColor Yellow }; Copy-Item scripts/init_database.sql $deployPath -Force; if (Test-Path scripts/init_sample_data.sql) { Copy-Item scripts/init_sample_data.sql $deployPath -Force }; if (Test-Path packages/快速部署指南.md) { Copy-Item packages/快速部署指南.md $deployPath -Force }; Write-Host "✅ 文件复制完成" -ForegroundColor Green