初始化数据库
# 重新初始化数据库（这会删除现有数据）
wsl -e bash -c "mysql -u root -proot < scripts/init_database.sql"
wsl -e bash -c "mysql -u root -proot < scripts/init_sample_data.sql"


内网启动

wsl -e bash -c "cd /mnt/d/Project/boshi/boshi-inspection-platform && source /opt/ros/humble/setup.bash && export ROS_DOMAIN_ID=0 && export HOST=0.0.0.0 && python3 -m app.main"
启动后内网  http://172.20.147.149:8000/docs 即可登录doc页面测试


外网穿刺

需要另开一个terminal，进入wsl后         ssh -R 80:localhost:8000 serveo.net
会获得一个临时url供外网访问

