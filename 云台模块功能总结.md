# 云台相关模块功能与参数总结

## 一、模块架构

云台模块采用分层架构，包含以下核心实体：

```
Gimbal (云台设备)
  ├── GimbalTask (云台任务)
  │   ├── GimbalInspectionProject (云台巡检项目)
  │   │   └── GimbalInspectionProjectPresetPoint (关联表)
  │   │       └── GimbalPresetPoint (预设点)
  │   └── GimbalSchedule (任务日程)
  └── GimbalHistory (巡检历史记录)
```

## 二、各模块详细说明

### 1. Gimbal (云台设备) - `tb_gimbal`

**功能**：管理云台设备的基础信息和连接参数

**核心参数**：

#### 基础信息
- `gimbal_name` (String, 100): 云台名称
- `map_id` (String, 36, 可选): 关联地图ID（用于在地图上显示云台位置）
- `enabled` (Boolean): 启用状态（默认：True）

#### 网络连接参数
- `ip_address` (String, 45): 云台IP地址
- `port` (Integer): 云台端口
- `username` (String, 100): 登录用户名
- `password` (String, 255): 登录密码

#### 视频流参数
- `rtsp_main_url` (String, 255): RTSP主码流地址
- `rtsp_sub_url` (String, 255, 可选): RTSP子码流地址
- `channel` (Integer, 1-2): 通道号（默认：1）

#### 地图坐标
- `x_coordinate` (Float, 可选): X坐标（地图横坐标）
- `y_coordinate` (Float, 可选): Y坐标（地图纵坐标）

#### 云台设备预设坐标（设备级别）
- `p_coordinate` (Float, 可选): P坐标（水平旋转）
- `t_coordinate` (Float, 可选): T坐标（垂直旋转）
- `z_coordinate` (Float, 可选): Z坐标（变焦）
- `f_coordinate` (Float, 可选): F坐标（聚焦）

**关系**：
- 一个云台可以有多个任务（`gimbal_tasks`）
- 一个云台可以有多个预设点（`preset_points`）

---

### 2. GimbalTask (云台任务) - `tb_gimbaltask`

**功能**：定义云台执行的任务，包含多个巡检项目

**核心参数**：
- `task_name` (String, 100): 云台任务名称
- `gimbal_id` (String, 36): 所属云台ID

**关系**：
- 属于一个云台（`gimbal`）
- 包含多个巡检项目（`inspection_projects`），按 `sort_order` 排序
- 可以有多个执行日程（`schedules`）

---

### 3. GimbalInspectionProject (云台巡检项目) - `tb_gimbal_inspection_project`

**功能**：定义具体的巡检项目，通过关联预设点来执行巡检

**核心参数**：
- `task_name` (String, 100): 巡检项目名称
- `gimbaltask_id` (String, 36): 关联云台任务ID
- `sort_order` (Integer): 排序值，数字越小越靠前（默认：0）

**说明**：
- 巡检项目本身不包含PTZF坐标和相机参数
- 所有执行参数都通过关联的预设点（`GimbalPresetPoint`）提供
- 一个巡检项目可以关联多个预设点（多对多关系）

**关系**：
- 属于一个云台任务（`gimbal_task`）
- 可以关联多个预设点（`preset_points`，通过中间表）
- 可以有多个历史记录（`histories`）

---

### 4. GimbalPresetPoint (云台预设点) - `tb_gimbal_preset_point`

**功能**：定义云台的预设位置和相机参数，可被多个巡检项目复用

**核心参数**：

#### 基础信息
- `preset_name` (String, 100): 预设点名称
- `gimbal_id` (String, 36): 关联云台ID
- `image_url` (String, 500, 可选): 图片链接

#### PTZF 坐标参数（相机控制）
- `p_coordinate` (Float, 可选): P坐标（水平旋转/Pan）
- `t_coordinate` (Float, 可选): T坐标（垂直旋转/Tilt）
- `z_coordinate` (Float, 可选): Z坐标（变焦/Zoom）
- `f_coordinate` (Float, 可选): F坐标（聚焦/Focus）

#### 相机拍摄参数
- `aperture` (Integer, 0-100, 可选): 光圈
- `shutter` (Integer, 可选): 快门分母

#### 图像处理参数
- `backlight_compensation` (Boolean): 背光补偿（默认：False）
- `wide_dynamic` (Boolean): 宽动态（默认：False）
- `strong_light_suppression` (Boolean): 强光抑制（默认：False）
- `fill_light` (Boolean): 补光（默认：False）

**关系**：
- 属于一个云台（`gimbal`）
- 可以被多个巡检项目关联（`inspection_projects`，通过中间表）

**说明**：
- 预设点存储了完整的相机控制参数
- 同一个预设点可以被不同的巡检项目使用，但可以有不同的检测类型和拍摄时长

---

### 5. GimbalInspectionProjectPresetPoint (关联表) - `tb_gimbal_inspection_project_preset_point`

**功能**：实现巡检项目与预设点的多对多关系，并存储检测类型和拍摄时长

**核心参数**：
- `inspection_project_id` (String, 36): 云台巡检项目ID
- `preset_point_id` (String, 36): 云台预设点ID
- `detection_type` (String, 20): 检测类型
  - 可选值：`可见光视频` / `可见光图片` / `热成像图片` / `热成像视频`
- `video_duration` (Integer, 可选): 拍摄时长（秒）
  - 当 `detection_type` 为 `可见光视频` 或 `热成像视频` 时使用

**唯一约束**：
- 同一个巡检项目 + 同一个预设点 + 同一个检测类型 的组合必须唯一

**说明**：
- 一个巡检项目可以关联多个预设点
- 同一个预设点可以在同一个巡检项目中使用多次，但检测类型必须不同
- 例如：一个巡检项目可以同时拍摄"可见光图片"和"热成像图片"

---

### 6. GimbalSchedule (云台任务日程) - `tb_gimbalschedule`

**功能**：定义云台任务的执行计划和时间安排

**核心参数**：

#### 基础信息
- `gimbaltask_id` (String, 50): 关联的云台任务ID
- `schedule_name` (String, 200): 日程名称
- `start_date` (Date): 开始日期
- `end_date` (Date): 结束日期
- `enabled` (Boolean): 启用状态（默认：True）

#### 执行周期配置
- `cycle_type` (String, 20): 周期类型
  - 可选值：`daily`（每天）/ `monthly_days`（每月指定日期）/ `weekly`（每周）/ `interval`（间隔）
- `cycle_config` (JSON, 可选): 周期详细配置（JSON格式）
  - 例如：`{"days": [1, 5, 10]}` 表示每月1、5、10日

#### 执行时间配置
- `time_mode` (String, 20): 时间模式
  - 可选值：`custom`（自定义时间段）/ `interval`（间隔执行）
- `time_config` (JSON): 时间详细配置（JSON格式）
  - 例如：`{"start": "08:00", "end": "18:00", "interval": 60}` 表示8点到18点，每60分钟执行一次

#### 显示字段（便于查询和展示）
- `frequency_display` (String, 200, 可选): 周期显示文本，例如："每天"、"每月1/5/10日"
- `time_display_start` (Time, 可选): 开始时间（用于时间轴显示）
- `time_display_end` (Time, 可选): 结束时间（用于时间轴显示）

**关系**：
- 属于一个云台任务（`gimbal_task`）

---

### 7. GimbalHistory (云台巡检历史记录) - `tb_gimbalhistory`

**功能**：记录云台巡检项目的执行结果

**核心参数**：
- `inspection_project_id` (String, 36): 关联云台巡检项目ID
- `record_data` (JSON, 可选): 记录数据（JSON格式，可存储各种巡检结果数据）
- `media_url` (String, 500, 可选): 图像/视频链接

**关系**：
- 属于一个云台巡检项目（`inspection_project`）

---

## 三、数据流程

### 执行流程

1. **创建云台设备** → 配置网络参数、RTSP地址等
2. **创建预设点** → 设置PTZF坐标、相机参数、图像处理参数
3. **创建云台任务** → 定义任务名称，关联到云台
4. **创建巡检项目** → 定义项目名称，关联到任务，设置排序
5. **关联预设点** → 为巡检项目关联预设点，指定检测类型和拍摄时长
6. **创建任务日程** → 设置任务的执行计划和时间安排
7. **执行巡检** → 系统按日程执行任务，调用预设点参数进行拍摄
8. **记录历史** → 保存巡检结果到历史记录表

### 数据关系示例

```
云台001 (Gimbal)
  └── 任务A (GimbalTask)
      ├── 巡检项目1 (GimbalInspectionProject)
      │   ├── 关联预设点1 → 可见光图片
      │   └── 关联预设点1 → 热成像图片
      └── 巡检项目2 (GimbalInspectionProject)
          └── 关联预设点2 → 可见光视频 (30秒)
      
  预设点1 (GimbalPresetPoint)
    ├── PTZF: (0, 0, 1.0, 0.5)
    ├── 光圈: 40, 快门: 60
    └── 图像处理: 背光补偿=ON, 宽动态=ON
  
  预设点2 (GimbalPresetPoint)
    ├── PTZF: (5.0, 2.5, 2.5, 1.2)
    ├── 光圈: 55, 快门: 30
    └── 图像处理: 背光补偿=ON, 强光抑制=ON, 补光=ON
```

---

## 四、关键设计特点

### 1. 预设点复用
- 预设点独立于巡检项目，可以被多个巡检项目复用
- 同一个预设点可以在不同巡检项目中使用不同的检测类型

### 2. 参数分离
- 云台设备参数：网络连接、RTSP地址
- 预设点参数：PTZF坐标、相机参数、图像处理参数
- 关联表参数：检测类型、拍摄时长

### 3. 灵活的执行配置
- 巡检项目只定义名称和排序，不包含具体参数
- 通过关联预设点来获取执行参数
- 支持一个项目关联多个预设点，实现多角度、多模式拍摄

### 4. 完整的调度系统
- 任务日程支持多种周期类型和时间模式
- 可以灵活配置执行计划

---

## 五、API接口

### 云台相关接口
- `GET /api/v1/gimbals/` - 获取云台列表（包含任务、日程、预设点）
- `GET /api/v1/gimbals/{gimbal_id}` - 获取云台详情
- `POST /api/v1/gimbals/` - 创建云台
- `PUT /api/v1/gimbals/{gimbal_id}` - 更新云台
- `DELETE /api/v1/gimbals/{gimbal_id}` - 删除云台

### 云台任务相关接口
- `GET /api/v1/gimbaltasks/` - 获取任务列表
- `POST /api/v1/gimbaltasks/` - 创建任务
- `PUT /api/v1/gimbaltasks/{task_id}` - 更新任务
- `DELETE /api/v1/gimbaltasks/{task_id}` - 删除任务

### 云台巡检项目相关接口
- `GET /api/v1/gimbalinspectionprojects/` - 获取巡检项目列表（包含关联的预设点）
- `POST /api/v1/gimbalinspectionprojects/` - 创建巡检项目
- `PUT /api/v1/gimbalinspectionprojects/{project_id}` - 更新巡检项目
- `DELETE /api/v1/gimbalinspectionprojects/{project_id}` - 删除巡检项目

### 云台预设点相关接口
- `GET /api/v1/gimbalpresetpoints/` - 获取预设点列表
- `POST /api/v1/gimbalpresetpoints/` - 创建预设点
- `PUT /api/v1/gimbalpresetpoints/{preset_id}` - 更新预设点
- `DELETE /api/v1/gimbalpresetpoints/{preset_id}` - 删除预设点

### 云台日程相关接口
- `GET /api/v1/gimbalschedules/` - 获取日程列表
- `POST /api/v1/gimbalschedules/` - 创建日程
- `PUT /api/v1/gimbalschedules/{schedule_id}` - 更新日程
- `DELETE /api/v1/gimbalschedules/{schedule_id}` - 删除日程

### 云台历史记录相关接口
- `GET /api/v1/gimbalhistories/` - 获取历史记录列表

---

## 六、参数说明

### PTZF 坐标说明
- **P (Pan)**: 水平旋转角度，控制云台左右转动
- **T (Tilt)**: 垂直旋转角度，控制云台上下转动
- **Z (Zoom)**: 变焦倍数，控制镜头焦距
- **F (Focus)**: 聚焦距离，控制镜头对焦

### 检测类型说明
- **可见光视频**: 使用可见光摄像头录制视频
- **可见光图片**: 使用可见光摄像头拍摄图片
- **热成像图片**: 使用热成像摄像头拍摄图片
- **热成像视频**: 使用热成像摄像头录制视频

### 图像处理参数说明
- **背光补偿**: 自动调整曝光以补偿背光
- **宽动态**: 扩展动态范围，改善明暗对比
- **强光抑制**: 抑制强光干扰
- **补光**: 开启补光灯

---

## 七、总结

云台模块采用预设点复用的设计模式，将相机控制参数（PTZF、光圈、快门等）集中在预设点表中，巡检项目通过关联预设点来获取执行参数。这种设计具有以下优势：

1. **参数复用**：同一个预设点可以被多个巡检项目使用
2. **灵活配置**：一个巡检项目可以关联多个预设点，实现多角度拍摄
3. **参数集中**：所有相机参数集中在预设点，便于管理和维护
4. **扩展性强**：通过中间表可以灵活配置检测类型和拍摄时长

